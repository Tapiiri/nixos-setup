"""User-facing CLI implementations.

Modules under this package implement the UX/arg parsing for commands that are
intended to be invoked by humans (either via `scripts/*` wrappers or flake apps).
"""
